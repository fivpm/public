Application is used for analyzing Windows firewall logs which can be first save to logs folder and from there they
can be loaded to Mongodb after which they are shown on application and visualized by charts.js. Registration is needed
in order to use application (credentials (hashed password) are saved to Mongo).